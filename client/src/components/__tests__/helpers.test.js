import { capFirstChar } from '../../utils/helpers';



// TODO: TEST BLOCK
//* render component to test
//* find elements to interact with
//* interact with said elements
//* assert that results are as expected



test('Capitalize first character of string', () => {
    let expectedVal = 'Test';
    expect(capFirstChar('test')).toBe(expectedVal);

});


test('test', () => {
    expect(true).toBe(true);
});
